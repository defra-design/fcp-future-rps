<div class="uc-section-card">
    <h2 class="uc-section-card__header">
        <a href="{{ params.titleUrl }}" class="uc-section-card__link">{{ params.title }}</a>
    </h2>

    {% if params.description %}
        <p class="uc-section-card__description">{{ params.description }}</p>
    {% endif %}
</div>
